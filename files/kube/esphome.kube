[Unit]
Description=ESPHome container run by Podman
Before=local-fs.target
After=network.target

[Kube]
Yaml=pod-esphome.yml
UserNS=keep-id
Network=host

[Install]
# Start by default on boot
WantedBy=multi-user.target default.target
